import {createTheme} from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4297dd',
    },
    secondary: {
      main: '#f50057',
    },
  },
  spacing: 8,
});

export default theme;
